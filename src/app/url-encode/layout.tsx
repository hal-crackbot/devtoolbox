import type { Metadata } from "next";
export const metadata: Metadata = { title: "URL Encode/Decode - DevToolbox", description: "Encode and decode URL components online for free." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
