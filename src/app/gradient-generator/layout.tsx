import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Gradient Generator - DevToolbox", description: "Create beautiful CSS gradients with a visual picker. Copy CSS instantly." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
