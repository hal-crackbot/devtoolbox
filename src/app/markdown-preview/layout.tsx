import type { Metadata } from "next";
export const metadata: Metadata = { title: "Markdown Preview - DevToolbox", description: "Side-by-side Markdown editor and preview. Free online tool." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
