import type { Metadata } from "next";
export const metadata: Metadata = { title: "Base64 Encode/Decode - DevToolbox", description: "Encode and decode Base64 strings online for free." };
export default function L({ children }: { children: React.ReactNode }) { return children; }
